version: 2
jobs:
  build:
    docker:
      - image: arthurphilippe/criterion
    steps:
      - checkout
      - run:
          name: build
          command: make
      - run:
          name: build unit tests
          command: make tests
  tests:
    docker:
      - image: arthurphilippe/criterion
    steps:
      - run:
          name: unit tests
          command: ./unit_tests.out
